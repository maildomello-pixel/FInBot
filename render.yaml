services:
  - type: web
    name: finbot
    env: python
    pythonVersion: "3.11.9"
    buildCommand: |
      python --version
      pip install -r requirements.txt
    startCommand: python bot.py